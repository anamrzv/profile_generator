version: '3.8'
services:
  profile-gen:
    build: .
    restart: unless-stopped
    volumes:
      - ./out:/app/out
      - ./assets:/app/assets:ro
    ports:
      - "${HOST_PORT:-3111}:${CONTAINER_PORT:-3000}"
    environment:
      - NODE_ENV=production
      - PORT=${CONTAINER_PORT:-3000}
version: '3.8'
services:
  profile-gen:
    build: .
    volumes:
      - ./out:/app/out
      - ./assets:/app/assets:ro
    environment:
      - NODE_ENV=production
      - PORT=${CONTAINER_PORT:-3000}